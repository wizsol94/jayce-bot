# JAYCE FULL SYSTEM AUDIT
## WizTheory Engine / Jayce Scanner Architectural Audit

**Started:** May 26, 2026
**Last updated:** May 28, 2026
**Status:** Sections 1-7 complete + setup_grader deep-dive complete
**Methodology:** Code-grounded, no patches during audit, sequential, brutally honest

---

# TABLE OF CONTENTS

- [Section 1 — Discovery / Intake](#section-1)
- [Section 2 — Candle & Structure](#section-2)
- [Section 3 — Setup Classification](#section-3)
- [Section 4 — Validator Layer](#section-4)
- [Section 5 — RSI & Momentum Philosophy](#section-5)
- [Section 6 — Engine Layer](#section-6)
- [Section 7 — Visual Intelligence Layer](#section-7)
- [Setup Grader Deep-Dive](#setup-grader)
- [Master Severity Table](#severity)
- [Defining Architectural Truths](#truths)

---

# THE DEFINING ARCHITECTURAL TRUTH <a name="truths"></a>

Across all sections, one pattern dominates:

**Jayce evolved via ADDITIVE architecture — new systems were layered on top of old ones instead of replacing them.**

Confirmed across: classifiers, structure analyzers, breakout detectors, caches, validators, RSI systems, orchestration paths, calculate_rsi implementations.

**THREE PARALLEL GRADING PIPELINES run on every token, then reconcile via ENGINE OVERRIDE:**

| Pipeline | Structure Source | Swing Algo | RSI Philosophy | Grade Thresholds | Age |
|---|---|---|---|---|---|
| **engines** | engines.analyze_structure | max/min (naïve) | legacy thresholds + divergence | A=75, A+=85 | OLDER |
| **bangers** | structure_engine.analyze_structure | fractal (lookback=3) | rsi_memory (doctrine) | A=85, A+=95 | NEWER |
| **impulse_detector** | internal | wick-cluster | peak_rsi (aligned) | (setup_detected bool) | MIDDLE |

**The root cause of pipeline disagreement:** The 3 pipelines use 3 DIFFERENT structure sources → they see DIFFERENT swing highs/lows → different impulse legs → different everything.

**They disagree on chart REALITY, not just opinion.**

ENGINE OVERRIDE (scanner.py:3164) lets OLDER engines pipeline overrule NEWER doctrine-aligned bangers when engine grade A/A+ > bangers grade. **Philosophical inversion: the inferior pipeline wins ties.**

**The future fix is NOT to delete pipelines.** It's SHARED PRIMITIVES (same swing detection, fib anchoring, structure source, RSI doctrine, grading foundations) underneath multiple analytical perspectives.

---

# SECTION 1 — DISCOVERY / INTAKE <a name="section-1"></a>

## Architecture Map

```
DexScreener (3 URLs):
  - TRENDING (max 200)
  - VOL_5M (max 50)
  - VOL_1H (max 50)
  All filtered: minLiq=10000, minMarketCap=100000, minAge=1
        ↓
vps_scraper.py (Playwright)
  - Interval: 300s
  - Webshare proxy
        ↓
queue.db.token_queue (SQLite)
        ↓
scanner.py:fetch_volume_from_queue (line 349)
  - Reads WHERE processed=0
        ↓
hybrid_intake.py
  - Stage 2: metadata score, top 60
  - Stage 3: gate computation
        ↓
engines.run_detection
```

## Filter cascade

**dexscreener_fetcher.py:**
- MIN_LIQUIDITY = 10000
- MIN_MARKET_CAP = 100000
- MIN_AGE_HOURS = 1

**scanner.py:**
- MIN_MARKET_CAP = 0 (DISABLED)
- MIN_LIQUIDITY = 10000
- MIN_COIN_AGE_HOURS = 3 (stricter than fetcher)
- MIN_CANDLES = 12

## Stage 3 gate computation (hybrid_intake.py)

Each gate computed independently:

| Gate | Territory | Criteria |
|---|---|---|
| passes_382fz_gate | 28-45% retrace | (ath_breakout OR major_high_break) AND has_valid_flip_zone AND expansion ≥ 60% |
| passes_50fz_gate | 40-60% retrace | (ath_breakout OR major_high_break) AND has_valid_flip_zone AND expansion ≥ 60% |
| passes_618fz_gate | 55-70% retrace | (ath_breakout OR major_high_break) AND has_valid_flip_zone AND expansion ≥ 60% |
| passes_786fz_gate | 68-82% retrace | (ath_breakout OR major_high_break) AND has_valid_flip_zone AND expansion ≥ 100% |
| passes_underfib_gate | ≥40% retrace | (ath_breakout OR major_high_break) AND has_valid_flip_zone AND expansion ≥ 60% |

**Default:** if token not in territory, gate defaults to True (passes).

## Storage

Three SQLite databases:
- `queue.db` (180KB) — token queue
- `jayce.db` (848KB) — main bot state
- `jayce_memory.db` (192KB) — alert memory / dedup

## Critical Findings (Section 1)

**1.A — Cooldown blocks setup evolution (HIGH, USER-CONFIRMED BUG)**
- User wants 382 → 50 → 618 → 786 evolution
- Current cooldown blocks all evolution
- See Section 6.E for full implementation detail

**1.B — Triple-duplicate 786 gate block (LOW)**
- Lines 612, 632, 666 in hybrid_intake.py
- Same code 3 times back-to-back
- Harmless but tech debt

**1.C — MIN_MARKET_CAP=0 dead checks (LOW)**
- scanner.py lines 1249, 1322, 1371, 1499
- Variable set to 0, then checked (always passes)
- Dead conditional logic

**1.D — Age filter mismatch with fetcher**
- fetcher: MIN_AGE_HOURS = 1
- scanner: MIN_COIN_AGE_HOURS = 3
- Different but intentional (defense-in-depth)

---

# SECTION 2 — CANDLE & STRUCTURE <a name="section-2"></a>

## Architecture Map

**Candle fetching (candle_provider.py):**
- Primary: Birdeye (requires TOKEN address)
- Fallback: GeckoTerminal (requires PAIR address)
- Daily Birdeye cap: 5000
- Gecko 429 cooldown: 60s

**Structure analyzers (FIVE+ parallel systems):**
1. `engines.analyze_structure` (line 258) — naïve max/min swings
2. `impulse_detector.measure_expansion` — wick-cluster impulse
3. `psef.find_swings` (lookback=3 fractal)
4. `structure_engine.analyze_structure` — fractal swings, used by bangers
5. `bangers_pipeline` orchestration

**Breakout detection (TWO parallel systems):**
1. `breakout_validator.validate_breakout` — historical resistance + 30% expansion
2. `impulse_detector.detect_breakout` — 50-candle wick-cluster
- Both consulted, can disagree (RICH/WYNN style failures documented)

**Cache systems (THREE with mismatched TTLs):**
| Cache | Tier1 | Tier2 | Tier3 |
|---|---|---|---|
| cache_tiers.py | 90s | 240s | 720s |
| candle_cache.py | NEVER | 90s | 120s |
| candle_provider legacy CANDLE_CACHE | 30min | — | — |

## Critical Findings (Section 2)

**2.A — 5+ parallel structure detection systems (HIGH)**
- Different algorithms produce different swing_high/swing_low
- Root cause of pipeline disagreement (See Section 7.A)

**2.B — structure_engine.py — CORRECTED (LOW)**
- Originally flagged as dead code in Section 2
- Section 7 discovered it's LIVE via bangers_pipeline.py:18
- Important correction documented

**2.C — Three cache systems with mismatched TTLs (MEDIUM)**

**2.E — queue.processed NEVER set to 1 (MEDIUM)**
- Confirmed via grep — never set anywhere
- Scanner re-chews same tokens until scraper refresh
- Cooldown is only dup-prevention mechanism

**2.F — engines.analyze_structure uses naïve max/min (HIGH)**
- Line 258: `max(highs)`, `min(lows)` across whole window
- Anchors old ATH for continuation setups
- BREAKS continuation behavior

**2.G — Two breakout detectors with different philosophies (HIGH)**
- breakout_validator (historical resistance + 30% expansion)
- impulse_detector (50-candle wick-cluster)
- Disagree on continuation setups

**2.H — run_detection 2 call sites (LOW — intentional)**
- process_token line 2888 = main path
- scan_top_movers line 3739 = whale path
- Different workflows, not duplicate

**2.J — psef.find_swings is 3rd swing detection copy (MEDIUM)**

**2.K — structural_prescan + hybrid_intake (LOW — sequential, complementary)**
- DEEP_SCAN / MONITOR / REJECT modes
- Not duplicate, sequential phases

**2.L — Underlying structure primitives duplicated across 5 modules (HIGH at analysis level)**

---

# SECTION 3 — SETUP CLASSIFICATION <a name="section-3"></a>

## Architecture Map

Two competing classifiers running on the same data:

**Classifier A — impulse_detector.py (FIB-CLASS)**
- Location: impulse_detector.py:754-770
- Wick-based match → body acceptance override
- Logs: `[FIB-CLASS] Wick-based match: {best_match}`
- Cannot classify Under-Fib (line 592 explicitly skips UNDER_FIB)

**Classifier B — engines.py (BODY-ROUTE)**
- Location: engines.py:585 `determine_setup_by_body_acceptance`
- Sophisticated multi-step:
  1. Fib levels (wick-based)
  2. Deepest wick retrace
  3. Body-percentile FZ detection
  4. Boundary rule
  5. Confidence scoring 0-100
  6. Special 382 handling (wick-origin, left-side rejection, thin FZ allowed)
- Logs: `[FLIP-ZONE] ✅ CLASSIFIED AS: {recommended_setup}`
- Drives engine selection via engine_order reordering

## Body acceptance override mechanism

engines.py:1324:
```python
body_recommended = (engine_id == recommended_setup 
                    and body_routing.get('confidence', 0) >= 50)
```

**At confidence ≥ 50 (half-confident), body acceptance overrides wick retrace range.**

Sprint B from prior planning was to raise this to 75.

## Three generations of body-acceptance code

| Location | Function | Status |
|---|---|---|
| engines.py:585 | `determine_setup_by_body_acceptance` | LIVE |
| engines.py:908 | `determine_setup_by_body_acceptance_OLD` | DEAD |
| impulse_detector.py:592 | `analyze_body_acceptance` | LIVE (different file) |

Two LIVE in different modules. Different scoring systems. Different point values.

## pseudo_overlap is proximity, not overlap

engines.py:881:
```python
pseudo_overlap = max(0, 100 - int(d * 4))
```

= `100 - 4*distance`. Distance-to-percent approximation labeled as "overlap_pct" in output.

## Critical Findings (Section 3)

**3.A — Two competing classifiers (HIGH)**
**3.B — `determine_setup_by_body_acceptance_OLD` dead code (LOW)**
**3.C — `pseudo_overlap` is proximity not overlap (LOW)**
**3.D — Body override threshold of 50 is sensitive (MEDIUM)**
**3.E — Hybrid gates vs engine ranges mismatch (LOW)**
**3.F — Triple-duplicate 786 block (upgrade from Section 1.B) (LOW)**
**3.G — Two `analyze_body_acceptance` functions in different files (HIGH)**
**3.H — Classifier A cannot classify Under-Fib (architectural)**
**3.I — MIXED-DEPTH warning doesn't gate alerts (MEDIUM)**

---

# SECTION 4 — VALIDATOR LAYER <a name="section-4"></a>

## Validator Inventory

| File | Size | Setup | Entry function |
|---|---|---|---|
| three_eighty_two.py | 17.3KB | 382 + Flip Zone | validate_382 |
| fifty_bounce.py | 10.3KB | 50 + Flip Zone | validate_50_bounce |
| six_eighteen.py | 9.2KB | 618 + Flip Zone | validate_618 |
| seven_eighty_six.py | 9.1KB | 786 + Flip Zone | validate_786 |
| under_fib.py | 15.9KB | Under-Fib | validate_under_fib |
| hunter_mode.py | 13.2KB | Shared | detect_expansion_exhaustion |
| breakout_validator.py | 7.9KB | Pre-gate | validate_breakout |

## ValidationResult dataclass (duplicated 5x)

```python
@dataclass
class ValidationLayer:
    layer_name: str
    passed: bool
    score: int
    reason: str
    weight: float = 1.0

@dataclass
class ValidationResult:
    setup: str
    passed: bool
    final_score: int
    final_grade: str
    layers: List[ValidationLayer]
    reasons_passed: List[str]
    reasons_failed: List[str]
    reject_reason: Optional[str]
    stage: int
    stage_label: str
    hunter_mode: bool
```

Each validator REDEFINES this. 5 copies of the same code.

## Hunter Mode signal map (detect_expansion_exhaustion)

| Signal | Threshold | Points | Status |
|---|---|---|---|
| Signal 1: rejection_wick | upper_wick > body × 1.5 | 25 | REAL |
| Signal 2: bearish_at_high | close < open | 20 | REAL |
| Signal 3: volume_spike | volume > 2× avg | 20 | REAL |
| Signal 4: pullback_started | price < swing_high × 0.97 | 15 | **AUTO-FIRES** any retraced token |
| Signal 5: rsi_divergence | rsi_at_high > 70 AND current rsi < 60 | 15 | **DEAD** |
| Signal 6: consecutive_red | 2+ red candles | 15 | REAL |

**Threshold (post-Sprint A):** `exhaustion_detected = exhaustion_score >= 55`

## Stale-breakout filter (THE CONTINUATION KILLER)

**breakout_validator.py:187:**
```python
MAX_CANDLES_SINCE_ATH = 100
if candles_since_ath > MAX_CANDLES_SINCE_ATH:
    # REJECT
```

100 candles × 5min = **8.3 hours**. Any setup where ATH > 8.3h ago = INSTANT REJECT.

**This kills RICH-style continuation setups.**

## Critical Findings (Section 4)

**4.A — Stale-breakout cap kills continuation setups (HIGH)**
- 100-candle / 8.3h limit
- Rejects clean continuation alerts
- Runs BEFORE setup validators

**4.B — Signal 4 in Hunter Mode is free 15 points (MEDIUM)**
- Fires for ANY token >3% below high
- Partially addressed by Sprint A (threshold 40→55)

**4.C — Signal 5 dead code (LOW)**
- rsi_at_high never populated
- Would be misaligned with doctrine if active

**4.D — ValidationResult dataclass duplicated 5x (LOW)**
- Tech debt, should be shared module

**4.E — validate_50_bounce has different signature (LOW)**
- Takes 2 args vs others' 3
- Rebuilds structure internally

**4.F — Hunter Mode is PRIMARY trigger for 382+50 (architectural)**
- Weight 1.5 for exhaustion layer
- Sprint A threshold change directly affects these setups

**4.G — 618/786 don't use Hunter Mode (architectural)**
- Use check_fib_break_with_mapping instead
- Different exhaustion philosophy
- Confirms Sprint A's targeted impact

**4.I — Validator layer weights not uniform (LOW)**

**4.J — Whale required for 786 — inconsistent? (MEDIUM, later resolved)**
- Resolved in Section 6.F: dead code path

---

# SECTION 5 — RSI & MOMENTUM PHILOSOPHY <a name="section-5"></a>

## The doctrinal anchor (bot.py:2133-2229)

```
RSI is NEVER a signal, prediction tool, or overbought/oversold indicator.
RSI is ONLY momentum memory and permission.

RSI ZONES (behavioral, not predictive):
  - RSI > 50:  Momentum supports continuation
  - RSI 40-50: Momentum intact; chop/acceptance bullish
  - RSI < 40:  Momentum damage
  - RSI < 30:  Trend integrity compromised (NOT "oversold")

RSI RULES:
  1. RSI describes PERMISSION, not direction
  2. RSI is for expectation management, not entries
  3. RSI confirms SURVIVAL, not entries

Order: Structure → RSI Permission → Divergence (ALWAYS LAST)
```

## Six calculate_rsi implementations

| File | Returns | Used by |
|---|---|---|
| engines.py:178 | single float | engines.analyze_structure |
| impulse_detector.py:102 | list | impulse_detector + Peak RSI |
| psef.py:32 | list | psef.gate_4_rsi |
| rsi_memory.py:35 | list | bangers_pipeline |
| runner_intelligence.py:40 | list | UNCLEAR |
| chart_intelligence.py:870 | list | chart_intelligence |

Same algorithm, six implementations.

## RSI usage classification

**🟢 ALIGNED with doctrine:**
- bot.py philosophy (the anchor)
- rsi_memory.py (Mode 1 Memory + Mode 2 Expansion)
- psef.py Gate 4 (2-phase: Phase 1 ≥65, Phase 2 ≥40)
- chart_intelligence.py momentum tiers (60+/45+/35+)
- Peak RSI infrastructure (3-tier fallback in scanner.py)

**🟡 SOFT-GATING (gray):**
- engines.py:484-488 RSI score tiers
- engines.py:1372 impulse + RSI combo gate

**🔴 MISALIGNED:**
- engines.py:494,546,560 rsi_divergence as primary signal
- hunter_mode.py:117 Signal 5 (DEAD but would violate doctrine)

## Critical Findings (Section 5)

**5.A — Six calculate_rsi implementations (MEDIUM)**
**5.B — Signal 5 misaligned (LOW — dead)**
**5.C — rsi_divergence used as primary signal in engines.py (MEDIUM)**
- bot.py says: divergence ALWAYS LAST
- engines.py uses as primary scoring input

**5.D — engines.py RSI thresholds predate doctrine (MEDIUM)**

**5.E — PSEF Gate 4 is philosophically cleanest gate (WIN)**
- Phase 1: confirm impulse via RSI peak ≥65
- Phase 2: confirm pullback survival via RSI ≥40
- Hard rejects only when momentum genuinely dead

**5.F — rsi_memory.py is doctrinal twin in code (WIN)**
- Mode 1 (Memory): 40-50 floor
- Mode 2 (Expansion): 70+ pressure, 75-90 runner
- Direct map of trading philosophy

**5.G — Peak RSI infrastructure aligned (WIN)**

**5.H — chart_intelligence.py tiers match doctrine (WIN)**

**5.I — Three "Phase 1 + Phase 2" implementations converge on same thresholds (WIN)**

---

# SECTION 6 — ENGINE LAYER <a name="section-6"></a>

## ENGINE_PARAMS (the source of truth, with STRICT_MODE=false confirmed)

| Engine | retracement_min | retracement_max | impulse_min | invalidation_fib | grade_threshold | whale_required |
|---|---|---|---|---|---|---|
| 382 | 30 | 40 | 30 | 0.50 | 70 | False |
| 50 | 40 | 55 | 50 | 0.618 | 70 | False |
| 618 | 50 | 65 | 60 | 0.786 | 70 | False |
| 786 | 70 | 80 | 100 | 0.786 | 75 | False |
| underfib | 55 | 85 | 60 | 0.90 | 70 | False |

**STRICT_MODE in .env: `false`** → no 1.2x multiplier active. Documented thresholds = production reality.

## run_detection 16-step orchestration

```
1.  analyze_structure(candles) → structure
2.  Add gate flags from token to structure
3.  check_whale_activity → has_whale
4.  determine_setup_by_body_acceptance → body_routing
5.  Reorder engine_order with recommended_setup first
6.  For each engine_id:
    a. Check cooldown (BUGGED: hardcoded 4h, token-only scope)
    b. CHECK 1: Retracement range OR body override (confidence ≥50)
    c. CHECK 2: Impulse minimum (no override)
    d. CHECK 3: Invalidation price below key fib
    e. CHECK 4: Whale required (with engine-specific overrides — mostly DEAD)
    f. CHECK 5: Under-fib special pass
    g. Call setup validator
    h. Flashcard analysis + grade boost (engines path)
    i. Grade threshold check
    j. Calculate entry zone
    k. Calculate invalidation
    l. Build engine_result dict
    m. Append to results
7.  If no results → return None
8.  Return max(results, key='score')
9.  set_engine_cooldown(address, best['engine_id'])
```

## The cooldown bug (engines.py:121-138)

```python
def get_cooldown_key(token_address: str, engine_id: str) -> str:
    # Comment: "allow setup evolution (382 → 618 → 786)"
    return f"{token_address}:STRUCTURE"  # engine_id IGNORED

def is_engine_on_cooldown(token_address, engine_id):
    cooldown_hours = 4  # HARDCODED — ignores ENGINE_PARAMS.cooldown_hours
```

**Intent:** allow setup evolution.
**Implementation:** blocks ALL engines for 4h once ANY one fires for a token.
**Effect:** 382 firing at 12:00 blocks 618 from firing at 14:00 even if it's a valid setup.

## ENGINE OVERRIDE mechanism (scanner.py:3164)

```python
if engine_result and engine_result.get('grade') in ['A+', 'A']:
    if bangers_result['grade'] not in ['A+', 'A']:
        # ENGINE OVERRIDE: bump bangers grade to engine grade
        new_score = max(engine_score, bangers_result['score'])
        if engine_grade == 'A+': new_score = max(new_score, 95)
        elif engine_grade == 'A': new_score = max(new_score, 85)
        bangers_result['grade'] = engine_grade
        bangers_result['engine_override'] = True
```

**ENGINE OVERRIDE is BANGERS pipeline accepting ENGINE's verdict when BANGERS independently grades lower.**

## Score → Grade mismatch between pipelines

| Grade | engines.py | bangers (setup_grader) |
|---|---|---|
| A+ | 85 | 95 |
| A | 75 | 85 |
| B+ | (no B+) | 75 |
| B | 65 | 65 |

**A 76-point score = A in engines = B+ in bangers.** Drives override frequency.

## Critical Findings (Section 6)

**6.A — THREE pipelines, not two (HIGH — architectural correction)**
- engines + impulse_detector + bangers
- ENGINE OVERRIDE reconciles engines vs bangers

**6.B — engines.py vs bangers grade thresholds differ (HIGH)**
- Direct cause of override frequency

**6.C — ENGINE OVERRIDE is healthy reconciliation (WIN)**
- Intentional architecture, not workaround

**6.D — Cooldown_hours config is dead code (MEDIUM)**
- ENGINE_PARAMS field exists but is_engine_on_cooldown hardcodes 4h

**6.E — Cooldown blocks setup evolution (HIGH — CONFIRMED BUG)**
- Same as Section 1.A
- engine_id ignored in cooldown key

**6.F — 786 violent mode RSI<35 override is DEAD code (LOW)**
- engines.py:1372
- Only reachable when whale_required=True
- But whale_required=False for 786 → unreachable
- User doctrine concern resolved: not running in production

**6.G — STRICT_MODE=false confirmed (audit confirmation)**

**6.H — 13+ sequential quality checks before alert (architectural)**

**6.I — Three different impulse_min definitions (MEDIUM)**

**6.J — Override score floor inflates scores to 85/95 (LOW)**

**6.K — flashcard_analysis adds another scoring layer (open — Section 7)**

---

# SECTION 7 — VISUAL INTELLIGENCE LAYER <a name="section-7"></a>

## THE CRITICAL FINDING: BANGERS structure source

**bangers_pipeline.py:18:**
```python
from structure_engine import analyze_structure
```

**Three pipelines use THREE DIFFERENT structure sources:**

| Pipeline | Structure Source | Swing Algorithm |
|---|---|---|
| engines | engines.analyze_structure | max/min (naïve) |
| bangers | structure_engine.analyze_structure | fractal (lookback=3) |
| impulse_detector | internal (measure_expansion) | wick-cluster |

**They see different swing highs/lows on the same chart.**
**They disagree on REALITY before they disagree on PHILOSOPHY.**

This is the architectural root of ENGINE OVERRIDE frequency.

## BANGERS pipeline composition

```
run_bangers_analysis(candles, psef_result, engine_result, flashcard_similarity):
  STEP 1: structure_engine.analyze_structure(candles)
          → trend, swings (FRACTAL), BOS, structure_grade
  STEP 2: rsi_memory.analyze_rsi_full(candles, trend, structure_grade)
          → RSI_MEMORY_INTACT, BREAKOUT_PRESSURE, RUNNER_MODE
  STEP 3: candle_intelligence.analyze_candles(candles)
          → tagged candles, character summary
  STEP 4: flashcard pattern matching
  STEP 5: setup_grader.grade_setup(...)
          → final BANGERS grade + score + should_alert
```

**BANGERS = structure_engine + rsi_memory + candle_intelligence + flashcard + setup_grader**

All doctrine-aligned components.

## Flashcard analysis (numeric similarity, grade boost)

**flashcard_analysis.py** (used in ENGINES path):
- Setup-specific comparison (50+FZ only vs 50+FZ flashcards)
- Max 10 flashcards per comparison
- Returns grade_boost: 0, 1, or 2 levels
- Can change A → A+
- Located: engines.py imports `analyze_flashcard_similarity, apply_grade_boost`

## Matcher architecture (setup_matcher_adapter.py)

**Current status:** SHADOW MODE for normal operation, ACTIVE GATE via Vision Fallback Option C when Vision unavailable.

Scanner.py:3004-3024 — shadow mode logging:
```python
_matcher_result = match_setup(chart_bytes, wiz_setup_type, top_n=5)
token['matcher_result'] = _matcher_result
```

Scanner.py:3396-3423 — Vision Fallback Option C (active gate):
```python
# When Vision unavailable, matcher Quality becomes the visual confirmation floor
if quality good → ALLOWED
elif quality borderline → ALLOWED WITH CAUTION
elif quality poor → REJECTED (no visual confirmation)
```

**Since Vision is currently $0 credits, matcher IS actively gating alerts now.**

## Vision integration

- DAILY_VISION_CAP = 250
- VISION_MIN_GRADE = 'B' (gates Vision calls)
- VISION_WEIGHT = 0.20 (advisory, not dominant)
- VISION_COOLDOWN_MINUTES = 45
- Graceful $0 degradation working

**Vision is properly positioned: advisory layer, gated by grade, with fallback.**

## Alert types (3 distinct)

1. **WIZTHEORY ALERT** — main path (format_wiztheory_alert)
2. **FORMING SETUP** — scanner.py:2385 (FORMING_THRESHOLD=40)
3. **CONFIRMED** — CONFIRMED_THRESHOLD=50

## Critical Findings (Section 7)

**7.A — THREE different structure sources (HIGH — the root cause)**
- Pipelines see different chart reality
- This is the architectural foundation of disagreement

**7.B — structure_engine.py NOT dead (corrects Section 2.B)**
- Live via bangers_pipeline

**7.C — Matcher more live than "shadow" suggests (MEDIUM)**
- Active gate during current Vision-less operation

**7.D — Flashcard grade boost can change alert outcomes (MEDIUM)**
- Boost of 2 → A → A+

**7.E — Philosophical inversion: newer pipeline overridden by older (HIGH)**
- BANGERS (doctrine-aligned) gets overridden by engines (legacy logic)

**7.F — Vision properly positioned (WIN)**

**7.G — setup_grader.py is BANGERS final grader (covered in deep-dive)**

**7.H — candle_intelligence.py LIVE via bangers (correction)**

**7.I — Three alert types exist (documented)**

---

# SETUP GRADER DEEP-DIVE <a name="setup-grader"></a>

## File: /opt/jayce/setup_grader.py (350 lines)

**Used by:** bangers_pipeline.py:21
**Called as:** `grade_setup(psef_result, structure_result, rsi_result, candle_summary, flashcard_result)`
**Returns:** `{grade, score, should_alert, breakdown, reason}`

**It's the final BANGERS grading brain.**

## Weighting (docstring vs actual code)

| Component | Docstring | Actual Code | Match? |
|---|---|---|---|
| PSEF | 20 | 20 | ✓ |
| Structure | 30 | 30 | ✓ |
| RSI Memory | 15 | **8** | ✗ (half) |
| RSI Expansion | 10 | **5** | ✗ (half) |
| Candle Quality | 15 | 15 | ✓ |
| Flashcard | 10 | 10 | ✓ |
| **Total max** | **100** | **88** | drift |

**RSI gets HALF the weight documented.** Score normalizes to 100 via `(total/max)*100` so percentages work, but relative weight of RSI vs other components is half.

## Actual weight percentages (of 88 max)

- PSEF: 22.7%
- Structure: 34.1% ← PRIMARY
- RSI Memory: 9.1% (intended 15%)
- RSI Expansion: 5.7% (intended 10%)
- Candle Quality: 17.0%
- Flashcard: 11.4%

## RSI doctrine alignment (the cleanest in codebase)

setup_grader.score_rsi uses ONLY:
- `RSI_MEMORY_INTACT` — Memory mode flag
- `memory_grade` — Memory quality grading
- `RSI_RUNNER_MODE` — Sustained strength
- `RSI_BREAKOUT_PRESSURE` — Pressing resistance

**ZERO usage of:**
- rsi_divergence
- Hard RSI thresholds
- Reversal-predictive logic
- Overbought/oversold language

**This is the gold-standard RSI implementation in the entire codebase.**

## BANGERS alert condition (triple gate)

```python
should_alert = (
    grade in ['A+', 'A'] AND
    score >= 85 AND
    psef_passed
)
```

Compared to engines.py: `score >= grade_threshold (70-75)` → fire.

**BANGERS is STRUCTURALLY STRICTER than engines.**

A 76 score:
- engines: A → fires
- bangers: B+ → no alert
- ENGINE OVERRIDE: bangers bumped to A

## Component scorers (the actual logic)

**score_psef:** Binary 0 or 20. PSEF passes all 4 gates or zero.

**score_structure:** `letter_to_points(grade, 30)`. Pure passthrough of structure_engine grade.

**score_rsi:** Memory (max 8 via letter_to_points) + Expansion bonus (5 for Runner, 3 for Breakout Pressure).

**score_candles:**
- TRENDING: 10 pts, COILING: 7 pts, MIXED: 3 pts
- +3 if expansion_count ≥ 2
- +2 if sweep_count ≥ 1
- Capped at 15
- **`rejection_count` extracted but never used — DEAD VARIABLE**

**score_flashcard:** Linear scaling (similarity/100) * 10.

## should_realert function

```python
def should_realert(previous_grade, new_grade):
    """Only re-alert if grade improved."""
    grade_rank = {'A+': 5, 'A': 4, 'B+': 3, 'B': 2, 'C': 1, 'D': 0}
    return new_rank > prev_rank
```

**Exists but live status unclear** — need to grep scanner for usage.

## Three pipelines comparison (with setup_grader insight)

| Dimension | engines | bangers | impulse_detector |
|---|---|---|---|
| Structure source | engines.analyze_structure | structure_engine.analyze_structure | internal wick-cluster |
| RSI philosophy | Hard thresholds + divergence | Memory + Expansion + Runner | Peak RSI |
| Grade thresholds | A=75, A+=85 | A=85, A+=95 | (boolean) |
| Alert condition | score ≥ grade_threshold | grade A/A+ AND score≥85 AND psef_passed | setup_detected |
| Strictness | Loose | **Strictest** | (binary) |
| Doctrinal alignment | Partial | **Highest** | Partial |

## Critical Findings (Setup Grader Deep-Dive)

**SG.A — Weights don't match docstring (RSI underweighted) (MEDIUM)**

**SG.B — `rejection_count` is dead variable (LOW)**

**SG.C — BANGERS is stricter than engines (triple gate) (HIGH)**

**SG.D — Two flashcard scoring systems run in parallel (MEDIUM)**
- engines: apply_grade_boost (tier bump)
- bangers: score_flashcard (0-10 numeric pts)

**SG.E — BANGERS RSI is cleanest doctrinal implementation (WIN)**

**SG.F — Structure is BANGERS' dominant signal (architectural)**
- 34% of total weight
- structure_engine grading not yet audited

**SG.G — PSEF reinforces RSI doctrine doubly (architectural elegance)**
- Direct: 13 pts max
- Indirect via Gate 4: portion of 20 PSEF pts
- Total RSI influence: ~20% of grade

**SG.H — should_realert logic exists but live status unclear (open)**

**SG.I — Grade thresholds differ from engines (HIGH)**
- Confirms Section 6.B with code reference

## The big architectural insight

**BANGERS is organically becoming the future core intelligence layer.**

Evidence:
1. Component sourcing (doctrine-aligned modules)
2. RSI doctrine purity
3. Score composition (structure-first, RSI permission, behavior confirmation)
4. Strictness alignment (triple gate)
5. Architectural separation (parallel implementation, not patch)

But: **BANGERS depends on structure_engine.py grading.** That module hasn't been audited yet — its grading algorithm is the single biggest input to BANGERS final grade.

---

# MASTER SEVERITY TABLE <a name="severity"></a>

## HIGH severity

| Finding | Section |
|---|---|
| Cooldown blocks setup evolution (CONFIRMED BUG) | 1.A + 6.E |
| 5+ parallel structure detection systems | 2.A |
| engines.analyze_structure uses naïve max/min | 2.F |
| Two breakout detectors with different philosophies | 2.G |
| 5-stage filter chain with duplicate primitives | 2.L |
| Two competing classifiers (FIB-CLASS vs BODY-ROUTE) | 3.A |
| Two body-acceptance functions with different scoring | 3.G |
| Stale-breakout cap kills continuation setups (RICH bug) | 4.A |
| THREE pipelines, not two (architectural correction) | 6.A |
| engines.py vs bangers grade thresholds DIFFER | 6.B + SG.I |
| THREE different structure sources (root cause) | 7.A |
| Philosophical inversion: newer overridden by older | 7.E |
| BANGERS triple-gate stricter than engines single-threshold | SG.C |

## MEDIUM severity

| Finding | Section |
|---|---|
| Three cache systems with mismatched TTLs | 2.C |
| queue.processed never set | 2.E |
| psef.find_swings is 3rd swing detection copy | 2.J |
| Body acceptance override threshold of 50 too permissive | 3.D |
| MIXED-DEPTH warning doesn't gate alerts | 3.I |
| Signal 4 in Hunter Mode is free 15 points | 4.B |
| Six calculate_rsi implementations | 5.A |
| rsi_divergence used as primary signal in engines.py | 5.C |
| engines.py RSI thresholds predate doctrine | 5.D |
| Cooldown_hours config is dead code | 6.D |
| Three different impulse_min definitions | 6.I |
| Matcher more live than "shadow" suggests | 7.C |
| Flashcard grade boost can change alert outcomes | 7.D |
| Weights don't match docstring (RSI underweighted) | SG.A |
| Two flashcard scoring systems run in parallel | SG.D |

## LOW severity

| Finding | Section |
|---|---|
| run_detection 2 call sites (intentional) | 2.H |
| structural_prescan + hybrid_intake sequential | 2.K |
| structure_engine.py is dead code (CORRECTED to LIVE) | 2.B |
| determine_setup_by_body_acceptance_OLD dead | 3.B |
| Triple-duplicate 786 gate block | 1.B + 3.F |
| MIN_MARKET_CAP dead checks | 1.C |
| Age filter mismatch | 1.D |
| pseudo_overlap is proximity not overlap | 3.C |
| Hybrid gates vs engine ranges mismatch | 3.E |
| Signal 5 dead code in Hunter Mode | 4.C + 5.B |
| ValidationResult dataclass duplicated 5x | 4.D |
| validate_50_bounce signature difference | 4.E |
| Validator layer weights not uniform | 4.I |
| 786 violent mode RSI<35 override is DEAD code | 6.F |
| Override score floor inflates scores to 85/95 | 6.J |
| `rejection_count` is dead variable | SG.B |

## WINS (architecturally aligned, don't disturb in cleanup)

| Finding | Section |
|---|---|
| PSEF Gate 4 is philosophically clean | 5.E |
| rsi_memory.py mirrors doctrine perfectly | 5.F |
| Peak RSI infrastructure aligned | 5.G |
| chart_intelligence.py tiers match doctrine | 5.H |
| Three "Phase 1 + Phase 2" implementations converge | 5.I |
| ENGINE OVERRIDE is healthy reconciliation | 6.C |
| Vision properly positioned (advisory, gated, fallback) | 7.F |
| BANGERS RSI is cleanest doctrinal implementation | SG.E |
| PSEF reinforces RSI doctrine doubly | SG.G |

---

# OPEN QUESTIONS

1. structure_engine.py grading algorithm — biggest BANGERS input, not yet audited
2. Is `should_realert` actually called in production?
3. setup_grader WEIGHTS: 88 total intentional or bug from someone tweaking weights?
4. Two flashcard systems (engines tier-bump vs bangers numeric) — which is correct per intent?
5. What % of production alerts have classifier disagreement (override rate measurement)?
6. Should rsi_divergence usage in engines.py be reordered after structure check?
7. Is the stale-breakout 100-candle cap right for continuation setups?
8. runner_intelligence.py — live or dead? What downstream consumes it?
9. Are setup_grader's WEIGHTS supposed to total 88 or 100?

---

# UPCOMING SECTIONS

- **Section 8 — Alert Layer** (next)
  - WIZTHEORY vs FORMING vs CONFIRMED alert types
  - Cooldown / dedupe / variety system
  - Caption assembly and chart rendering
  - Three-pipeline grade collision and final alert decision
  - Telegram routing logic
  - should_realert verification

- **Section 9 — Logging / Monitoring / Safety**
  - Watchdog cron behavior
  - Heartbeat infrastructure
  - Silent failure modes
  - Daily summaries
  - Observability gaps

- **Section 10 — Architecture Review (the synthesis)**
  - Shared primitives strategy
  - Prioritized action list across all findings
  - Consolidation roadmap (what to keep, refactor, remove)
  - The BANGERS-as-core-layer migration plan

---

# THE KEY DOCTRINE LOCKS (user philosophy, preserved)

- Setup evolution 382 → 50 → 618 → 786 must be allowed (cooldown bug must be fixed)
- RSI = permission/momentum-memory, NEVER prediction/reversal/divergence-predictor
- Under-Fib labels: "Under-618 Flip Zone" / "Under-786 Flip Zone"
- Flip zone = "flip zone" never "neckline"
- Future architecture = shared primitives under multiple analytical perspectives (NOT delete pipelines)
- BANGERS appears to be the doctrine-aligned core; consolidation likely means engines adopting bangers' primitives
- Vision/matcher/flashcard architecture is healthy — graceful degradation, advisory not dominant
- 618/786 fib-break philosophy vs 382/50 Hunter-Mode-exhaustion philosophy is INTENTIONAL and correct
- Audit is mapping mode: no patches, document everything, prioritize action at end (Section 10)

---

*Audit document — preserved across context compactions.*
*Living document — updated after each section completion.*
